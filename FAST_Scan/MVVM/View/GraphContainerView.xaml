<UserControl x:Class="FAST_Scan.MVVM.View.GraphContainerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FAST_Scan.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="4*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <ContentControl Grid.Row="0"
                        Content="{Binding CurrentGraphView}"
                        Margin="10"/>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Button Name="ExpandWindowButton"
                    Content="Expand Window" 
                    Grid.Column="0"
                    MinWidth="80"
                    MaxWidth="150"
                    Height="30"
                    HorizontalAlignment="Stretch"
                    Margin="10"/>

            <Button Name="SaveImageButton"
                    Content="Save Image" 
                    Grid.Column="1"
                    MinWidth="80"
                    MaxWidth="150"
                    Height="30"
                    HorizontalAlignment="Stretch"
                    Margin="10"/>

            <Button Name="GraphConfigurationsButton"
                    Content="Configurations" 
                    Grid.Column="2"
                    MinWidth="80"
                    MaxWidth="150"
                    Height="30"
                    HorizontalAlignment="Stretch"
                    Margin="10"/>
        </Grid>
    </Grid>
</UserControl>
